# AI Service

This service provides AI-powered analysis for the Startup Pilot platform.

## Setup
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```
